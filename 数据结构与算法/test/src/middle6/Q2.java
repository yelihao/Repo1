package middle6;

public class Q2 {
    /**
     * 给一个数组（有正有负）
     * 求一个数组中连续区间内的最大值
     */

    /**
     * @param
     *      int cur：一开始等于0
     *      int max：一开始等于系统最小
     * 遍历数组：当前数累加到cur上去后 cur=cur+arr[i]
     *          max跟踪一下此时cur的值，更大了则更新
     *          如果cur<0则cur=0，其他条件则cur不变
     */
    /*
    就是从头遍历，如果cur<0，说明之前的数加了不如不加，大于等于0则保留，一直重复和max比较即可
     */

    /**
     * 给定一个矩阵
     * 返回此矩阵子矩阵最大累加和
     */
    /**
     * 求解方法：
     */






}
